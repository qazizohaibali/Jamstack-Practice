import * as React from 'react'

import ServicesAnimatedSection from './ServicesAnimatedSection'

export default {
  title: 'Sections/ServicesAnimatedSection',
  component: ServicesAnimatedSection,
}

const Template = (args) => <ServicesAnimatedSection {...args} />

export const Default = Template.bind({})
Default.args = {}
