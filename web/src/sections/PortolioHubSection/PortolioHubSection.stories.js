import * as React from 'react'

import PortolioHubSection from './PortolioHubSection'

export default {
  title: 'Sections/PortolioHubSection',
  component: PortolioHubSection,
}

const Template = (args) => <PortolioHubSection {...args} />

export const Default = Template.bind({})
Default.args = {}
