import * as React from 'react'

import CustomAnimatedCursor from './CustomAnimatedCursor'

export default {
  title: 'Components/CustomAnimatedCursor',
  component: CustomAnimatedCursor,
}

const Template = (args) => <CustomAnimatedCursor {...args} />

export const Default = Template.bind({})
Default.args = {}
